# Contact form email configuration
CONTACT_EMAIL=Business@florensservices.com

# SMTP settings (required for the contact form to send emails)
# Example for Gmail / Google Workspace:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=Business@florensservices.com
# SMTP_PASS=your_app_password

# For production, use a transactional email service like Resend, SendGrid, or AWS SES.
